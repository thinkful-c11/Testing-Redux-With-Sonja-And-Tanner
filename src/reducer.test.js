import Reducer from './reducer';
import {makeGuess, MAKE_GUESS, toggleInfoModal, TOGGLE_INFO_MODAL, newGame, NEW_GAME} from './actions';

describe("Reducer", () => {
  it("newGame should reset state.", ()=>{

  })
})